# Backlog

What has been raised and not yet done. Priorities reflect who asked most recently.

| ID | Title | Area | Owner | Notes |
| :- | :- | :- | :- | :- |
| `ISSUE-50` | Figures in a pack carry no source | packs | Lucia Ferreira | "When someone asks in November why the July figure was what it was, I cannot answer." `POLICY-13` already requires this. |
| `ISSUE-51` | No record that a pack was reviewed | packs | Lucia Ferreira | `POLICY-14` requires a second reader. There is nowhere to record one, so it has never been enforceable. |
| `ISSUE-52` | Self-service and automation rate may be the same number | packs | Henrik Sole | Finance calls it one thing, service delivery calls it another. `REQUEST-011` with analytics is unanswered. |
| `ISSUE-53` | Percentages are not re-checked after inputs change | packs | | Raised after `INCIDENT-04`. Nobody has looked at whether the other two July packs have the same problem. |
| `ISSUE-54` | Nothing measures article age | articles | Ana Fialho | `POLICY-04` asks for re-verification past 180 days. `ARTICLE-0139` is 237 days old and still published. |
| `ISSUE-55` | The index build publishes articles missing source or reviewer | articles | Priya Nair | `POLICY-02` says those are not eligible. `ARTICLE-0131` is one and it is the only cycle-count article. |
| `ISSUE-56` | Pack layout could change without the consumers knowing | packs | Henrik Sole | Finance pulls three fixed cells by hand and stopped checking a year ago. |
| `ISSUE-57` | August packs for ACCOUNT-1003 and ACCOUNT-1008 not started | packs | Lucia Ferreira | Deadline 2026-09-04. |
| `ISSUE-58` | Move the packs to the new template branding | packs | | Carried over from the marketing refresh. The template was never produced. |
| `ISSUE-59` | Retire ARTICLE-0156 properly | articles | Priya Nair | Already `retired` in the index. The request is to remove it from the source tree, which nothing depends on. |
