# Backlog

What has been raised and not yet done. Priorities reflect who asked most recently.

| ID | Title | Area | Owner | Notes |
| :- | :- | :- | :- | :- |
| `ISS-50` | Figures in a pack carry no source | packs | `P-LUC` | "When someone asks in November why the July figure was what it was, I cannot answer." `POL-13` already requires this. |
| `ISS-51` | No record that a pack was reviewed | packs | `P-LUC` | `POL-14` requires a second reader. There is nowhere to record one, so it has never been enforceable. |
| `ISS-52` | Deflection and automation rate may be the same number | packs | `P-HEN` | Finance calls it one thing, service delivery calls it another. `REQ-011` with analytics is unanswered. |
| `ISS-53` | Percentages are not re-checked after inputs change | packs | | Raised after `INC-04`. Nobody has looked at whether the other two July packs have the same problem. |
| `ISS-54` | Nothing measures article age | articles | `P-ANA` | `POL-04` asks for re-verification past 180 days. `KB-0139` is 237 days old and still published. |
| `ISS-55` | The index build publishes articles missing source or reviewer | articles | `P-PRI` | `POL-02` says those are not eligible. `KB-0131` is one and it is the only cycle-count article. |
| `ISS-56` | Pack layout could change without the consumers knowing | packs | `P-HEN` | Finance pulls three fixed cells by hand and stopped checking a year ago. |
| `ISS-57` | August packs for ACC-1003 and ACC-1008 not started | packs | `P-LUC` | Deadline 2026-09-04. |
| `ISS-58` | Move the packs to the new template branding | packs | | Carried over from the marketing refresh. The template was never produced. |
| `ISS-59` | Retire KB-0156 properly | articles | `P-PRI` | Already `retired` in the index. The request is to remove it from the source tree, which nothing depends on. |
